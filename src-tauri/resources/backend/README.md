# Backend runtime bundle (PyInstaller onedir) is produced by:
#   ./scripts/bundle_backend.sh
#
# Expected layout after bundling:
#   offlinevoice-backend/offlinevoice-backend
#   offlinevoice-backend/_internal/...
#
# Model weights are intentionally NOT shipped here.
